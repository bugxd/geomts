export interface Point {
  x: number; y: number
};

export interface Circle {
  x: number,
  y: number,
  r: number
}

export interface Particle {
  x: number,
  y: number,
  speed: number,
  angle: number,
}
